{
  "name": "packituponly",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",

    "test:run": "coverage run manage.py test && npm --prefix frontend test -- --watchAll=false",

    "test:coverage": "coverage html && npm --prefix frontend test -- --coverage",

    "unseed": "python manage.py unseed"
  },
  "engines": {
    "node": "18.x"
  }
}
