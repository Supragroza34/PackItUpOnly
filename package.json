{
  "name": "packituponly",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",

    "test:run": "cd frontend && npm test -- --coverage --watchAll=false",

    "test:coverage": "cd frontend coverage html && npm --prefix frontend test -- --coverage",

    "unseed": "python manage.py unseed"
  },
  "engines": {
    "node": "18.x"
  }
}
